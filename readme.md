Fill the API or gemini and azure in .env file 
need to update the latest format of using gemini api or azure api key due to the latest update
type py app.py in terminal to run the app in local 
the app currently running the sql lite need to create a new database to replace this database 